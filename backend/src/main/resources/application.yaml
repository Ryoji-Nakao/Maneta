spring:
  application:
    name: maneta
  datasource:
    url: jdbc:postgresql://localhost:5432/maneta
    username: admin
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: your-secret-key-here-make-it-long
  expiration: 86400000